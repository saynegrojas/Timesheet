@model IEnumerable<Timesheet.Models.HourCode>

@{ ViewBag.Title = "Index"; }
@Html.Partial("_PartialNavbar");

<p class="text-right">
    <a href="@Url.Action("Logout", "Home")">Log out</a>
</p>
<h2>Hour Code</h2>
<p class="text-right">
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table table-striped table-hover">
    <tr>
        <th>
            @Html.DisplayName("Code Description")
        </th>
        <th>
            @Html.DisplayName("Code Value")
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
<tr>
    <td>
        @Html.ActionLink(item.CodeDescription, "Details", new { id = item.CodeID }, new { datatoggle = "tooltip", title = $"Click for details" })

    </td>
    <td>
        @Html.DisplayFor(modelItem => item.CodeValue)
    </td>
    <td>
        @Html.ActionLink("Edit", "Edit", new { id = item.CodeID }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.CodeID })
    </td>
</tr>}

</table>
