@model Timesheet.Models.Job_Role

@{ ViewBag.Title = $"Dr. {Model.JobDescription} Details"; }

@Html.Partial("_PartialLogout")

<h2><b>@Model.JobDescription</b> Details</h2>

<div>
    <hr />
    <dl class="dl-horizontal well">
        <dt>
            @Html.DisplayNameFor(model => model.JobDescription)
        </dt>
        <dd>
            @Html.ActionLink(Model.JobDescription, "Edit", new { id = Model.JobRoleID }, new { datatoggle = "tooltip", title = $"Click to edit {Model.JobDescription} role" })
        </dd>

    </dl>
</div>
<p>
    @*@Html.ActionLink("Edit", "Edit", new { id = Model.JobRoleID }) |*@
    @Html.ActionLink("Back to List", "Index")
</p>
